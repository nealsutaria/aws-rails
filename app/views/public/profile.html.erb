<div class="row">
    <div class="col-sm-8">
    <%= render partial: "posts/list", locals: { posts: @posts } %>
    </div>
    <div class="col-sm-4">
    <p class="h4"><%= @profile.username %></p>
    <p>Created day: <%= @profile.created_at.strftime("%m/%d/%Y") %></p>
    </div>
</div>
